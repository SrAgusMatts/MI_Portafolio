function Hero() {
    return (
        <section className="hero">
            <h1 className="hero-title">Agustín Mattos</h1>
            <p className="hero-subtitle">Desarrollador Web & Técnico en Programación</p>
            <a href="#projects" className="btn-primary">
                Ver Proyectos
            </a>
        </section>
    );
}

export default Hero;
